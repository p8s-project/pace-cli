package types

import "testing"

// TestTypes is a placeholder to satisfy the `go test` command.
func TestTypes(t *testing.T) {
	// This package contains only data structure definitions (structs).
	// The parsing and validation of these types are tested in the
	// internal/generator package.
}
