package main

import "testing"

// TestMain is a placeholder to satisfy the `go test` command.
func TestMain(t *testing.T) {
	// This package contains the main entrypoint of the application.
	// The logic is tested via the sub-packages (e.g., cmd, internal/generator).
}
